[project]
name = "fusion-cloud-mcp"
version = "0.1.0"
description = "Cloud MCP server for Autodesk Fusion Team via APS Data Management API"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
fusion-cloud-mcp = "mcp_server.server:main"
